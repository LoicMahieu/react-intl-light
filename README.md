# react-intl-light

A ligter version of `react-intl` that only export:

- `FormattedMessage`
- `FormattedHTMLMessage`
- `defineMessages`
- `IntlProvider`

## Instlal

```
npm install react-intl-light
```
